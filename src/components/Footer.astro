---
const today = new Date();
---

<footer>
	&copy; {today.getFullYear()} Davide Magliano · <a href="/agent">Agent view</a>
</footer>
<style>
	footer {
		padding: 2em 1em max(3.25em, env(safe-area-inset-bottom)) 1em;
		background: transparent;
		border-top: 1px solid rgba(var(--border-rgb), 0.7);
		color: rgb(var(--gray));
		text-align: center;
		font-size: 0.9em;
	}
	footer a {
		color: inherit;
	}
	footer a:hover {
		color: var(--accent);
	}
</style>
